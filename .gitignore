# Secrets and local config
.env

# Arweave-Cli: Node install only (.env covered by root `.env`)
Arweave-Cli/node_modules/

# Local documents
document (8).pdf

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/
# Root folder named `test` is a local venv in this repo (pyvenv.cfg)
/test/

# OS / editor noise
.DS_Store
Thumbs.db
*.log

# JSON outside data/ (artifacts, wallets, etc.)
**/*.json
!data/**/*.json
!**/package.json
!**/package-lock.json
